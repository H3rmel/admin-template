import { Button } from "@chakra-ui/react";

import { MainLayout } from "@/layouts/Index";

export default function Settings() {
  return (
    <MainLayout title="Configurações" subtitle="Bem-vindo de volta!">
      <Button colorScheme="primary">Tomi</Button>
    </MainLayout>
  );
}
